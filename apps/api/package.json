{
  "name": "@opentraces/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute opentraces --local --file src/db/schema.sql",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@opentraces/schema": "*",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250801.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  }
}
